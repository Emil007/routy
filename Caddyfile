# Only used when the "caddy" service in docker-compose.yml is uncommented.
# Domain is set there via the DOMAIN environment variable — nothing to edit
# in this file. Caddy takes care of obtaining and renewing the Let's Encrypt
# certificate automatically.

{$DOMAIN} {
	reverse_proxy routy:3000
}
