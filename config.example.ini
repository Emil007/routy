[db]
host = 127.0.0.1
port = 3306
user = routy
password = secret
database = routy


# SwiftTileserverCache StaticMap-Endpoint (ggf. mit Style-Parametern)
# Beispiel: http://127.0.0.1:8080/staticMap?style=basic
static_base_url = http://127.0.0.1:8080/staticMap
width = 900
height = 600
zoom = 15

# provider: simple (default) | tileservercache
provider = simple
# For tileservercache-style paths: http://host:port/ (root)
static_provider_url = http://127.0.0.1:8080/
# map_type: staticmap | multistaticmap (if your tileserver supports it)
map_type = staticmap
# template used after 'poracle-' when provider=tileservercache; e.g. 'route'
template = route
# Optional query key appended to static map requests
api_key = 
# Optional auth header added when fetching image (e.g., Authorization / X-API-Key)
auth_header_name = 
auth_header_value = 

[routes]
# pro Klick auf 'Neue Route' wird die Toleranz um diesen Prozentwert erweitert
widen_step_percent = 5.0
# maximale zusätzliche Erweiterung der Toleranz
widen_max_percent = 30.0
# Gewichtung der tagesaktuellen Diversität (Penalty pro heute benutztem Segment)
daily_diversity_weight = 1.0
precalc_min_km = 1.0
precalc_max_km = 12.0
tolerance_percent = 10.0

[home]
home_name = Home

[ingest]
merge_radius_m = 50
name_far_warn_m = 300

[bot]
name = Routy
token = YOUR_DISCORD_BOT_TOKEN_HERE
# Optional: Guild-ID für schnellere Command-Sync (nur für diese Guild)
# guild_id =